const gallery1Slider = new Swiper('.gallery-1__swiper-container', {
    // Optional parameters
    loop: true,

    // If we need pagination
    pagination: {
        el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',

    },

    // And if we need scrollbar

})
const gallery2Slider = new Swiper('.gallery-2__swiper-container', {
    // Optional parameters
    loop: true,

    // If we need pagination
    pagination: {
        el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',

    },

    // And if we need scrollbar

})
const gallery3Slider = new Swiper('.gallery-7__swiper-container', {
    // Optional parameters
    loop: true,

    // If we need pagination
    pagination: {
        el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',

    },

    // And if we need scrollbar

})
const gallery4Slider = new Swiper('.gallery-8__swiper-container', {
    // Optional parameters
    loop: true,

    // If we need pagination
    pagination: {
        el: '.swiper-pagination',
    },

    // Navigation arrows
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',

    },

    // And if we need scrollbar

})

const gallery5Slider = new Swiper('.gallery-9__swiper-container', {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
    },
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',
    },
})

const gallery6Slider = new Swiper('.gallery-15__swiper-container', {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
    },
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',
    },
})

const gallery7Slider = new Swiper('.gallery-19__swiper-container', {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
    },
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',
    },
})
const gallery8Slider = new Swiper('.gallery-20__swiper-container', {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
    },
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',
    },
})
const gallery9Slider = new Swiper('.gallery-21__swiper-container', {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
    },
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',
    },
})

const gallery10Slider = new Swiper('.gallery-22__swiper-container', {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
    },
    navigation: {
        prevEl: '.swiper-prev',
        nextEl: '.swiper-next',
    },
})





